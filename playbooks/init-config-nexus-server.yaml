---
- name: Initial Nexus server configuration
  hosts: nexus_servers
  become: yes

  tasks:
    - import_tasks: ../tasks/update_all_packages.yaml

    - name: Reboot the system
      ansible.builtin.reboot:

    - import_role: 
        name: install_docker

    - import_tasks: ../tasks/pull_and_run_nexus_container.yaml

