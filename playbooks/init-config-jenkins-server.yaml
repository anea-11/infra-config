---
- name: Initial Jenkins server configuration
  hosts: jenkins_servers
  become: yes

  tasks:

    - import_tasks: ../tasks/update_all_packages.yaml

    - name: Reboot the system
      ansible.builtin.reboot:

    - import_role:
        name: install_jenkins

    - import_role: 
        name: install_docker
