---
- name: Initial Encoding server configuration
  hosts: encoding_servers
  become: yes

  tasks:
    - import_tasks: ../tasks/update_all_packages.yaml

    - name: Reboot the system
      ansible.builtin.reboot:

    - import_role: 
        name: install_docker

    - import_tasks: ../tasks/deploy-encoding-app.yaml
